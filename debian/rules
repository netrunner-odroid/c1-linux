#!/usr/bin/make -f
%:
	dh $@ --parallel

override_dh_auto_configure:
	make odroidc_defconfig
	dh_auto_configure

override_dh_auto_build:
	dh_auto_build
	make modules
	make uImage

override_dh_auto_clean:
	make clean

override_dh_auto_install:
	make modules_install INSTALL_MOD_PATH=debian/tmp/
